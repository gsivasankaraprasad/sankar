pipeline {
  agent any
  stages {
   stage ('build') {
     steps {
       echo " build is started"
       sh "mkdir file11"
      }
}

     stage ("deploy") {
       steps {
      echo "deployment is started"
       sh "touch file123"

}
}
}
}
