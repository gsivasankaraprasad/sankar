pipeline {
  agent any
  stages {
   stage ('build') {
     step {
       echo " build is started"
       sh "mkdir file11"
      }
}

     stage ("deploy") {
       step {
      echo "deployment is started"
       sh "touch file123"

}
}
}
}
